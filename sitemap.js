import {locations,resources,site} from "@/lib/site";
export default function sitemap(){const now=new Date();const staticRoutes=["","/custom-homes","/build-on-your-lot","/process","/projects","/resources","/locations","/contact"];return [...staticRoutes.map(route=>({url:`${site.url}${route}`,lastModified:now,changeFrequency:route===""?"weekly":"monthly",priority:route===""?1:.8})),...locations.map(l=>({url:`${site.url}/locations/${l.slug}`,lastModified:now,changeFrequency:"monthly",priority:.7})),...resources.map(r=>({url:`${site.url}/resources/${r.slug}`,lastModified:now,changeFrequency:"monthly",priority:.7}))]}
