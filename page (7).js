import Link from "next/link"; import Breadcrumbs from "@/components/Breadcrumbs"; import CTA from "@/components/CTA"; import {locations} from "@/lib/site";
export const metadata={title:"Custom Home Builders Across West Michigan",description:"Explore Snowden Builders’ proposed priority-market pages for custom homes and build-on-your-lot projects across West Michigan."};
export default function Page(){return <><section className="page-hero"><div className="container"><Breadcrumbs items={[{label:"Locations"}]}/><small className="copper">West Michigan</small><h1>Local pages built around real projects and real expertise.</h1><p>Each market page must contain distinct, builder-reviewed information. The site should not publish thin pages that merely swap city names.</p></div></section><section className="section"><div className="container grid grid-3">{locations.map(l=><article className="card" key={l.slug}><small>{l.county}</small><h3>{l.name}</h3><p className="muted">A proposed page focused on {l.angle}.</p><Link className="card-link" href={`/locations/${l.slug}`}>Explore {l.name} →</Link></article>)}</div></section><CTA/></>}
